import { makeStyles } from "@material-ui/core/styles";

const TextErrorStyle = makeStyles((theme) => {
    return {
        error: {
            color: "red",
            fontSize: "14px",
        },
    };
});
export default TextErrorStyle;
